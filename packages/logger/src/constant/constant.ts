import { LogLevel } from '@hyperse/logger-common';

export const defaultLoggerName = 'hps-logger';

export const defaultErrorLoggerName = 'hps-logger-error';

export const defaultLogLevel = LogLevel.Info;
